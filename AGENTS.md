# AGENTS.md — 課程講稿系列風格指南

這是林鼎淵（Dean Lin）的課程講稿庫。每個資料夾是一個客戶／平台的課程：`content.md`（講稿）＋ `config.yaml`（課程設定），由 `course-page-generator` skill build 成單頁 `index.html`。DSL 語法定義以 skill 的 SKILL.md 與 `reference/components.md` 為準；本文件記錄的是「怎麼用」的風格約定。

## 受眾光譜（新課程先定位）

技術深 → 淺：

| 課程 | 受眾 | 特徵 |
|------|------|------|
| `wiwynn/` | 企業工程師 | 最完整的開發流程內訓（環境、SDD、測試、CI/CD、Worktree） |
| `cht/vibecoding-*`、`tibame/lesson*` | 開發者系列課 | 三堂遞進，有回顧／預告／branch 標記 |
| `tku/` | 學生 | wiwynn 的精簡版 |
| `cake/` | 開發者社群演講 | 大綱式，少 lab-session |
| `eda/` | 企業一般員工 | Codex 桌面版（Connector/Project/Skill/Cowork），不碰 Git/CLI |
| `tsgh/` | 醫療零基礎 | 最重心法與比喻，大量 `[warning]`，強調「不確定就留白」 |

寫新課程時，先問受眾定位，選最接近的現有課程當結構參考。

## 三層分離（鐵律）

- `content.md` **沒有 frontmatter**：標題、SEO、開場／結尾金句、講者資訊一律不寫在講稿裡。
- 課程 `config.yaml` 只寫要覆蓋的欄位（`page` / `seo` / `quotes`），缺項回退 `config/global.yaml`。
- `nav` 省略不寫，由 `#` 章節自動生成。

## 標題階層

- `#` 主章節：**冒號式命題**「LABEL：一句賣點」（`# 常見痛點：穩定性不足、難以維護、無法驗證`），系列課可用「XX 篇」。章節之間用 `---` 分隔。附加單元（課後問題、補充資料）用 emoji 開頭跳過編號。
- `##` 小節：完整句或動賓短語，**不帶 emoji**。
- `###` 卡片：**一律語意化 emoji 開頭**（😓 痛點、🎯 目標、⚠️ 警告、✅ 正確、🆚 對比、🛠️ 實作、🛡️ 安全）。
- `####` 操作步驟細分（`#### STEP 1：…`）。

## 課程骨架

**開場**：痛點驅動——第一個 `#` 先立痛點、引發共鳴，再給解法。直播／說明會可用 `[interactive]` 投票暖場。

**中段**：每個操作小節的節奏是「情境說明 → 可複製的 ```prompt → lab-session 練習」。lab-session 的 title 依受眾命名：工程師「🛠️ 實作時間」、一般使用者「實戰演練」、學生「實作練習」；`hint` 慣用「有問題歡迎提出，你的問題可能是大家的問題」。

**收尾三件套（固定順序）**：
1. `[summary]`（格式 `- emoji **標題** | 一句話說明`）
2. `[bonus title="🎁 幕後製作心得"]`
3. `[qa-session title="Q&A 時間"]`（實體企業課可在前面加 `[survey]`）

## 系列課四件套

1. **回顧上次**：開頭「課前回顧／課後問題」承接上一堂，可主動修正上一堂講義的問題。
2. **預告下次**：結尾「🔮 下一堂預告」。
3. **`[time]` 遞進**：`active: true` 標記當前堂次，過去堂次補上 `link:` 講義連結。
4. **範例 repo／branch 遞進**：範例專案跨堂延續，用 `| branch:xxx` 標題標記分階段。

## 語氣與文字

- **人稱分工**：對學員說話用「你」，講自身經驗用「我」（現身說法），建立共識用「我們」。
- **口語、有幽默感**：允許情緒詞與玩笑（「AI 很擅長製造 Bug，自產自銷小意思」），但每大段末尾要收在一句可截圖傳播的金句 callout。
- **技術名詞保留英文**（Codex、Commit、Token、Skill），首次出現可「中文（English）」對照。
- **全形中文標點**；中英文之間加半形空格；箭頭統一 `→`；舉例用「（ex: …）」。
- **行內反引號當螢光筆**：`` `中文關鍵詞` `` 用於強調，不限程式碼——這是最高頻的排版習慣。
- 圖片的 alt 文字寫成完整圖說句子；路徑統一 `assets/`（不加 `./`）。
- code fence 語言標識一律小寫（`prompt`，不是 `Prompt`）。

## 教學手法

- **Insight box 自問自答**：`> **為什麼…？**` 問句開頭，後續行給答案。這是講者旁白的主要載體，密度要高。
- **比喻先行**：新技術名詞先給生活比喻再講定義（Git = 後悔藥、Docker = 標準化貨櫃、CI = 自動批改作業、Rules = 員工手冊、Skill = SOP）。
- **before/after 對比**：用表格或 `[tags]` 呈現「人工 vs AI」「過去 vs 現在」「沒有 X vs 有 X」。
- **真實對話還原**：進階敘事法（見 tsgh）——`#### STEP N` ＋ 當時的 prompt ＋「我當時為什麼這樣做」條列。
- **安全叮嚀反覆出現**：「不要無腦 Accept」「按下同意責任就是你的」；敏感場景（醫療、個資）用 `[warning]` 且強調「不確定就留白」。

## 禁止事項

- 不發明 SKILL.md 沒有的 DSL 標記。
- 不留裸段落——所有內容落在卡片、Insight、Flow、Tags 等結構化元件內。
- 不逐字轉錄講稿——移除口語過場詞，提煉成條列與卡片。
- 講者姓名是「鼎淵」（Dean Lin）；處理 ASR 逐字稿時注意「鼎源／鼎元」等同音錯字。
